{
  "name": "actrun-mcp",
  "version": "0.1.0",
  "description": "MCP server for actrun — AI agent harness with Intent Gate and structured inputs",
  "type": "module",
  "bin": {
    "actrun-mcp": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun src/cli.ts serve",
    "start": "bun src/cli.ts serve",
    "test": "bun test",
    "lint": "oxlint -c oxlintrc.json src/ test/",
    "check": "bun test && oxlint -c oxlintrc.json src/ test/",
    "setup": "bun src/cli.ts setup"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "gray-matter": "^4.0.3",
    "neverthrow": "^8.2.0",
    "valibot": "^1.3.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^1.57.0"
  }
}
